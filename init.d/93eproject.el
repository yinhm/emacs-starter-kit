(require 'eproject)
(require 'eproject-extras)

;; ruby
;;(define-project-type ruby (generic) 
;;  (look-for "Rakefile")
;;  :relevant-files ("\\.rb$"))

;; python
;(define-project-type python (generic) (or (look-for "setup.py") (look-for "__init__.py"))) :relevant-files ("\\.py$")) 

;; haskell
;;(define-project-type haskell (generic) (look-for "Setup.lhs"))

;; perl
;;(define-project-type perl (generic)
;;  (or (look-for "Makefile.PL") (look-for "Build.PL"))
;;  :relevant-files ("\\.pm$" "\\.t$" "\\.pl$" "\\.PL$"))

